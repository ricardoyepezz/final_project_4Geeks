import React, { useContext } from "react";
import { Context } from "../store/appContext";
import "../../styles/home.css";

export const Editar = () => {
    const Click = () => {

    }

return(
<button className="btn-warning" onClick={()=>Click}>Editar</button>
)}